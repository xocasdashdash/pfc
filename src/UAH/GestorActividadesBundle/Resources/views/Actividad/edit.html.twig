{% extends '::backend.html.twig' %}
{% block content %}
    <div class="container">
        <div class="row">
            {{ form_start(form, { 'action': path('uah_gestoractividades_actividad_edit',{'activity_id': activity.id}) }) }}

            {{ form(form) }}
            {{ form_widget(form._token) }}

            {{ form_rest(form) }}
            {{ form_end(form) }}

        </div>
    </div>
{% endblock %}